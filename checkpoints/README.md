# Checkpoints

The pretrained, task-specialized, and sparse merged checkpoints are available
from [GitHub Releases](https://github.com/taewon-seo/disjoint-parameter-training/releases/latest).

Download all six checkpoints into this directory with:

```bash
bash scripts/download_checkpoints.sh
```

Checkpoint binaries are kept outside the Git repository and retain their
original filenames.
